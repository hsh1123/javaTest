<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="com.example.googleproject.Item.repository.itemRepository">

    <select id="getCategori" resultType="categoriDto">
        SELECT * FROM categori
    </select>

    <select id="getPlace" resultType="placeDto">
        SELECT * FROM place
    </select>

    <select id="getPrice" resultType="priceDto">
        SELECT * FROM price
    </select>

</mapper>
